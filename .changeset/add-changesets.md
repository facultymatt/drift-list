---
"drift-list": patch
---

Add `@changesets/cli` for versioning and release management. Contributors now note user-facing changes with `npm run changeset`; releases are cut with `npm run version` and `npm run release`.
